# Railway Nixpacks Configuration
[phases.setup]
nixPkgs = ["python314"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT --timeout-graceful-shutdown 5"
